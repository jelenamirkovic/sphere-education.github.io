from mergexp import *
# create a topology named 'hashcollision'
net = Network('hashcollision', addressing==ipv4)
nodes = [net.node(name, image == 'bullseye-edu') for name in [ 'target' ]]
experiment(net)